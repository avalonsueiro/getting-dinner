# Google Places API key, used only by scripts/fetch-restaurants.ts at build time.
# Enable "Places API" in Google Cloud Console, then paste the key here in .env.
# This never reaches the browser — the script writes plain JSON and downloads photos.
GOOGLE_PLACES_API_KEY=
